# 系统主频设置 (ESP32-S3 支持 240MHz)
CONFIG_SYS_CLOCK_HW_CYCLES_PER_SEC=240000000

# 开启串口和控制台
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y

# 开启 GPIO 支持
CONFIG_GPIO=y

CONFIG_ESP_SPIRAM=y
CONFIG_SPIRAM_MODE_OCT=y

# 堆大小
CONFIG_HEAP_MEM_POOL_SIZE=30240
